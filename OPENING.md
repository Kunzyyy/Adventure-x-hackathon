# 新任务开场白（复制到 ChatGPT / 通义千问 的新任务里）

```
请先 read 项目根目录的 CONTEXT.md，了解 keal 项目背景和当前进度。

规则：
1. 不要让我贴代码全文或截图，需要看文件请用 read 工具自己读
2. 我只用文字描述需求，你负责 read 对应文件
3. 每次只做我要求的一件事，做完汇报，不擅自改其他文件
4. 对话超过 20 轮或上下文快满时，提醒我开新任务并重读 CONTEXT.md

读完 CONTEXT.md 后，告诉我你已经了解项目现状，然后等我下指令。
```

---

## 用法

1. 在 ChatGPT / 通义千问 里点 **「新建任务」**
2. 粘贴上面这段（从 ``` 到 ``` 之间的内容）
3. AI 会自己 read CONTEXT.md，然后等你下指令
4. 之后你说需求只用文字，别贴图、别贴代码

## 为什么这样能避开上下文超限

- 新任务历史为空（不是把旧历史重发一遍）
- AI 用 read 看文件，你不贴代码/截图（截图是 token 吞噬机）
- 典型任务 token 数从 135 万降到 ~5 万，远低于 105 万上限

## 配合做

CC Switch 里把 `deepseek-v4-pro` 改成 `deepseek-chat`（或 `deepseek-reasoner`）——这是桌面 app 里的设置，需你手动点。
